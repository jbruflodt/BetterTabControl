<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BetterTabs"
             xmlns:SampleDataSource="clr-namespace:Expression.Blend.SampleData.SampleDataSource" x:Class="BetterTabs.NewDataGrid"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <SampleDataSource:SampleDataSource x:Key="SampleDataSource" d:IsDataSource="True"/>
        <DataTemplate x:Key="ItemTemplate">
            <Grid Height="Auto" Width="Auto">
                <DataGridColumnHeader Content="DataGridColumnHeader" VerticalAlignment="Stretch"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="ItemTemplate1">
            <StackPanel>
                <TextBlock Text="{Binding Property1}"/>
                <CheckBox IsChecked="{Binding Property2}"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid DataContext="{Binding Source={StaticResource SampleDataSource}}" d:DataContext="{Binding Source={StaticResource SampleDataSource}}">
        <DataGridColumnHeadersPresenter ItemsSource="{Binding Collection}" ItemTemplate="{DynamicResource ItemTemplate}"/>
    </Grid>
</UserControl>
